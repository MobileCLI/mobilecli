# Phase 0 Baseline Report

Date collected: 2026-02-25 (UTC)
Plan reference: `docs/TERMINAL_OVERHAUL_FINAL_MASTER_PLAN_2026-02-24.md`
Raw harness output: `docs/PHASE0_HARNESS_REPORT.json`

## Environment

- Daemon endpoint: `ws://127.0.0.1:9847`
- Harness: `scripts/terminal-overhaul-harness.mjs`
- Wrapper: `scripts/terminal-overhaul-benchmark.sh`
- Scenario run: `all` (`attach_latency`, `reconnect_stress`, `duplicate_detector`)

## Commands Used

```bash
scripts/terminal-overhaul-benchmark.sh docs/PHASE0_HARNESS_REPORT.json
```

## Baseline Metrics

| Metric | Baseline | Target | Status |
|---|---:|---:|---|
| Attach subscribe-ack latency (P95) | 42.3ms | <300ms | Pass |
| Attach first stable frame latency (P95) | 42.2ms | <300ms | Pass |
| Reconnect loop count | 30 | 30 | Pass |
| Duplicate loop rate (reconnect_stress) | 1.0000 (30/30 loops) | 0 | Fail |
| Blank attach rate | 0.0000 (0/30 loops) | 0 | Pass |
| Total duplicate tokens (reconnect_stress) | 948 | 0 | Fail |

## Notes

- This harness currently focuses on attach/reconnect/duplicate behavior.
- Keyboard-only resize count and mobile perceived key-latency are not yet included in the automated harness output.
- Mobile input-latency instrumentation was added in `mobile/hooks/useSync.ts` and should be validated on device/log capture during Phase 0 completion.

## Immediate Implications

1. Duplicate output during reconnect is reproducible and severe (100% loop incidence in baseline).
2. Attach timing is already well within latency targets on local LAN.
3. Phase 1-2 should prioritize deterministic attach ordering (attach_id + sequence model) before other optimizations.
